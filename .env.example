# EmailJS — https://www.emailjs.com/docs/
# Restart dev server after changes: npm run dev
VITE_EMAILJS_SERVICE_ID=your_service_id
VITE_EMAILJS_TEMPLATE_ID=your_template_id
VITE_EMAILJS_PUBLIC_KEY=your_public_key

# Template variables sent by the app:
# name, from_name, email, phone, message, subject, time, reply_to
#
# In EmailJS dashboard, also set on your template:
# - "To email" → your Gmail address (e.g. info@dreamandcelebrate.am)
# - "From name" → Dream & Celebrate
# - Connect Gmail under Email Services and re-authorize if expired
